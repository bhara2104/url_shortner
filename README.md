URL Shortener Application in JAVA 

Added Redis Database to cache the responses instead of direct DB hits and Added Endpoints to create Shortened URL and Get the Original URL back from shortened URL 
